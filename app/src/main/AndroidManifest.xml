<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Passa">
        <activity android:name=".OnlineGame.RegisterActivity" />
        <activity android:name=".OnlineGame.LoginActivity" />
        <activity android:name=".OnlineGame.OnlineGameActivity" />
        <activity android:name=".OnlineGame.OnlineJoinGame" />
        <activity android:name=".OnlineGame.OnlineCreateGame" />
        <activity android:name=".OfflineGameMenu" />
        <activity android:name=".OnlineGame.OnlineGameMenu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StartUpMenu" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>